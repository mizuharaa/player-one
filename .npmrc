confirm-modules-purge=false
# Windows + React Native: expo-modules-core reaches react-native through a
# pnpm symlink, and CMake regenerates build.ninja forever ("still dirty after
# 100 tries"). A flat node_modules is what the Android build needs.
node-linker=hoisted
