# Warm landing, demo studio and sheets: independent QA

**Local and bounded public release verdict: PASS after fixes.** Independent review after builder freezes. No product implementation or financial mutation was authored by this QA lane. Browser sessions use the serial `withBrowser/newPage` helper. Public deployment evidence is recorded below.

## Completed local evidence

- `node apps/console/scripts/warm-independent-qa.mjs`: 18 reduced-motion layout cases, VI/EN/ZH at320,375,768,1440,1920 and1440x600. All have zero horizontal document overflow, one settled hero title, hidden introductory slogan and no page errors. Cookie banners captured open, then deliberately dismissed for lower-page inspection. Footer retains32 SVG paths and fits every viewport.
- `node apps/console/scripts/warm-interactions-qa.mjs`: six cases,375/1440 across VI/EN/ZH. Search returns no matches, clearing/filtering returns the expected illustrative task, details receives focus, both declarations start unset and preparation stays disabled until both are explicitly chosen. Final instruction names the physical Ego button. Reset clears search/filter/declarations. No API mutations. All six FAQ rows expand; prepared help answers open, Escape closes and returns focus. Glass/solid navigation has identical measured bounds and16px top inset.
- Two concrete CSS conflicts were found and fixed: Truc's pause control inherited the larger page button font and wrapped into three lines; centered demo/work headings inherited a more specific old left alignment. Recheck: pause112x44 with12px type; heading/body center difference at most0.008px in the six interaction cases.
- Truc's112px column initially retained a280px stage host, placing the canvas at x=-64 on375px and clipping it. Builder changed the stage and every fallback to112px. Final normal/reduced-motion checks place the visible stage and canvas at x20, width112, with a112x44 pause control; offscreen scheduled animation callbacks0 in both cases.
- `node apps/console/scripts/warm-scroll-independent-qa.mjs`: real wheel scrolling, rapid reversal, idle then4px movement, anchor navigation, hover/out, locale switching and resize at375/1440/1920.499 sampled states retain heading opacity1. No scene-container transforms remain; no offscreen movie is playing. Idle scheduled animation callbacks settle to0 in each width. Opening film plays after handoff. Headless results do not certify physical-device frame rate.
- `node apps/console/scripts/logo-continuity-independent-qa.mjs --compact`:32 unique unchanged paths,1.8s duration, exact geometry1.15s, no invalid samples, unordered times or final pose errors.192 interior knots; finite-difference derivative disagreement below0.024 at1e-6s. `warm-logo-independent-qa.mjs` checks91 timeline positions at375/1440/1920 with zero pieces outside the viewport; three ordinary loads/reloads contain32 paths and finish in1.93-1.98s including mount/import overhead. Reduced motion has no intro. Initial warm ink was identified and builder restored literal existing near-black. Final computed path fills are rgb(23,25,27) at0/1.15s and brand orange rgb(255,122,26)/blue rgb(74,133,248) at1.35s.

## Real demo studio and database isolation

`node apps/console/scripts/showcase-real-independent-qa.mjs` used real local demo authentication and a6581-byte synthetic2-second H.264 test-pattern clip. Upload201, actual playback advances0.409s with readyState4 and no media error, seek1.2s completes, Range bytes4-7 returns206 and exactly4 bytes with private/no-store. Demo review200 persists partial after reload; DELETE204 is followed by media404.375px document overflow0. Logout clears cookies. These were authorized local demo writes only, never a production review or financial action.

The first browser probe lost a DevTools response body during development refresh; it is not counted as a product failure. The rerun read the actual server list and completed. Its preliminary generated clip exposed a real UI deletion defect during final cleanup: the shared request helper attached JSON Content-Type to an empty DELETE, which returned400 and left media accessible200. The earlier API-only delete did not certify that button. Builder changed `apps/console/src/showcase/ShowcaseScreen.tsx` to add JSON Content-Type only when a body exists. `node apps/console/scripts/showcase-delete-ui-qa.mjs` then pressed the actual Delete clip button:204, Content-Type absent, media404. Both generated QA clips have now been removed. Historical `final-extras.json` retains the400 observation; `showcase-delete-ui.json` records the successful fix.

`node scratchpad/qa/showcase-test-run.mjs`: independent **9/9 API tests passed** after corrective0027, using a fresh isolated database with runtime role playerone_app. Covers byte signatures/ranges, own-owner access including same-centre peers, reviewer/collector/unauthenticated refusal, mutation header, expiry, owner/global quotas, competing database connections, audited actions and no production episode/review/settlement rows. The upgraded test reproduces default table privileges and asserts raw owner/content/expiry/filename UPDATE fails with42501 while demo-review fields work.

`node scratchpad/qa/showcase-independent-grants.mjs` verifies the actual existing local demo database after0027 as playerone_app: table-wide UPDATE false; operator_id/content/expires_at/filename UPDATE false; verdict/note/reviewed_at UPDATE true. The builder correctly identified that0026 alone had inherited broad default UPDATE; no raw-database immutability pass is attributed to that earlier migration.0027 explicitly revokes the broad grant before allowing the three review columns.

Evidence: `scratchpad/qa/warm-independent/showcase-real.json`, `showcase-grants.json`, `showcase-1440.png`, `showcase-375.png`. Test fixture databases are distinct from the operational local demo database. No production financial table was seeded or truncated.

## Sheets and populated settlement regression

The first native-dialog probe passed filters but exposed a hang on the populated settlement page: first Choose period/Escape completed, then the second pointer click stalled. A plain native dialog/date control passed. Author tracing identified repeated TanStack `resetPageIndex`/`onPaginationChange` dispatches from table rows memoized against the fresh `useQueries` result array. The final fix in `apps/console/src/payout/SettleScreen.tsx` uses structurally shared combined income data and a stable empty-bills array. Diagnostic Radix and period-component changes were reverted; the original native dialog remains.

`node apps/console/scripts/sheets-final-independent-qa.mjs` independently repeats the affected gates after the final fix.320EN/375VI/375ZH: native modal, initial focus inside, Tab reaches no background control, Escape restores the trigger, cancel discards drafts, Apply persists, Reset clears, body scrolling preserves the sheet,70px handle drag preserves it and110px dismisses it, no overflow. Native Tab traversal may temporarily report BODY while browser chrome has focus; it is not reported as uninterrupted element focus inside.768/1440 retain inline filters.375 finance performs three close/reopen cycles, edited Cancel/reopen resets the draft, and Apply navigates to the chosen period.768 retains the inline date field. No page errors or financial writes. Evidence: `scratchpad/qa/sheets-independent/final-results.json`, `final-filters-320-en.png`, `final-date-375.png`.

`node apps/console/scripts/settle-stability-independent-qa.mjs`: two populated real local finance rows. An isolated browser query-cache income fixture updates the joined valid-minutes cell; restoring the actual server response and refetching unchanged income retain responsiveness and repeated period opening. CDP ScriptDuration advances0 seconds over each bounded700ms idle sample, before and after refetch. No errors or financial writes. This checks the concrete loop; it is not a general performance benchmark. Evidence: `settle-stability.json`.

`node apps/console/scripts/payment-final-independent-qa.mjs`: manual and API modes use real local read-only bill data with browser-only mode/preflight fixtures. Every non-GET payout/settle/risk request is intercepted before navigation. Empty/wrong amount and missing manual reference disable submission. Dirty Escape/backdrop/Close cannot discard entry; no drag handle appears. Explicit Cancel clears fields and restores focus. Pending fields/buttons/Cancel are disabled and Escape/backdrop cannot dismiss. Exactly one intercepted `/mark-paid` and one `/pay` return deliberate409 refusals, displayed inside the sheet; dismissal works afterward. No payment request reaches the API. This is presentation acceptance, not proof of any payment execution. Evidence: `final-payment-results.json`, `final-payment-dirty-manual.png`, `final-payment-pending-api.png`.

## Final visual and build checks

`node apps/console/scripts/warm-final-extras-qa.mjs`: final Truc stage and literal-black logo checks above; no page errors. Computed primary action contrast is9.61:1 light/9.53:1 dark; selected filter contrast8.55:1 light/10.08:1 dark. Hover preserves those colors with opacity1. These are functional control colors; decorative logo orange/blue is evaluated separately. Screenshots: `scratchpad/qa/warm-independent/truc-final-normal.png`, `truc-final-reduced.png`, `sheet-action-light.png`, `sheet-action-dark.png`, `logo-final-register.png`.

Independent checks:

- `pnpm --filter @playerone/console typecheck` passed after the table fix; the final header-only Studio fix also passed the author's typecheck.
- `pnpm exec vitest run apps/console/src/payout/payout.test.ts apps/console/src/payout/gate.test.ts` and `pnpm exec vitest run apps/console/src/lib/format.test.ts apps/console/src/routes/episodes.test.ts`:46 existing tests passed across four files. These are separate from browser interaction evidence.
- `pnpm --filter @playerone/console build` passed after the final Studio deletion fix,7.27s. Vite reports its existing large-chunk warning for the application/3D assets; no build error.
- Independent showcase API9/9 and actual local runtime privilege checks passed as recorded above.

No unresolved blocking defect remains in this local scope. Physical-device smoothness is not certified; no real financial operation was tested or claimed.

## Public deployment acceptance

Deployment `6c53ca68-9747-4705-8244-b8d7a584adae`, `https://playerone-web-production.up.railway.app`, after the backend owner's separate deployed Studio test released the browser lane.

`QA_DEPLOYMENT=6c53ca68-9747-4705-8244-b8d7a584adae node apps/console/scripts/warm-public-independent-qa.mjs` completed desktop landing/login and mobile landing/login checks before a QA selector targeted the hidden drawer locale control instead of the visible mobile-header control. That timeout is a probe error, not a product defect. The corrected visible selector is `.workspace-mobile-controls select`; the remaining checks were run separately without repeating the landing matrix.

- Health200/ready true. Ordinary first load and reload retain32 paths at every sampled frame, no debug controls, initial rgb(23,25,27), final orange/blue. Observed durations1961ms/1983ms include mounting overhead; authored timeline remains1.8s. Film is playing after handoff, and the extra opening slogan has display:none.
- Warm navigation dimensions stay fixed across scrolling. Settled question heading remains opacity1 after tiny scroll/reverse. Actual native locale popup displays all three choices with espresso text on cream, rather than white text on white.
- Desktop1440 login panels measure720px each, title begins at159px and document overflow0.375 reduced-motion landing has no intro, Vietnamese selection persists across reload, and visible Truc canvas is112px wide within the viewport. Representative captures are in `scratchpad/qa/warm-public/`.
- `QA_DEPLOYMENT=6c53ca68-9747-4705-8244-b8d7a584adae node apps/console/scripts/warm-public-finance-qa.mjs` passes actual HTTPS finance sign-in200, both session cookies Secure/HttpOnly/SameSite Strict, visible mobile language selection and Chinese persistence after reload. Batch GET200 returns0 bills, expected for the unseeded cloud financial data. Repeated period close/reopen, edited Cancel/reset and Apply navigation pass. Logout leaves0 cookies and `/whoami`401. No financial writes and no page errors.

Evidence: `scratchpad/qa/warm-public/results.json` (completed desktop checks), `finance-results.json`, `landing-1440.png`, `locale-popup-1440.png`, `login-1440.png`, `login-375.png`, `truc-375.png`, `date-375.png`. The backend owner's separate deployed Studio result is `scratchpad/qa/showcase-production/results.json`; it is not attributed as this lane's independent execution. Browser closed; no remaining blocker in this bounded public scope.
