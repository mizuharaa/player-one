# White intro and Spline — independent QA, 2026-09-10

Status: white intro PASS; Spline corrections pending final recheck. No product edits or backend mutations by this auditor.

`node apps/console/scripts/white-intro-independent-qa.mjs`: 9/9 passed, exit0. Actual1440/375 font weight650, zero slot width drift; 2300ms text clock followed by327.8/333.5ms observed fade/removal. Start background rgb(255,252,246), ink rgb(53,39,31); final orange rgb(185,74,5) and blue rgb(15,85,204). Skip, Escape, real nav #demo while overlay present, scroll exit and initial reduced motion passed. Font rejection removed overlay in597ms; never-resolving font promise failed open in5351ms. No page errors. Evidence `scratchpad/qa/white-intro-independent/results.json` plus desktop/mobile white/film captures. The independent numeric timeline test also remains1/1 passing.

`node apps/console/scripts/spline-independent-qa.mjs`: real current production dist through `createShowcaseServer` with actual response CSP, ephemeral loopback port, software WebGL enabled via `--enable-unsafe-swiftshader`.1440/375 supplied scene ready; scene and process/physics WASM all200; only prod.spline.design/cdn.spline.design remote dependencies observed. Mobile corrected frame fits the HTML screen; no repeated hardware island or exposed wallpaper. Zero page overflow. All four actual HTML demo flows passed (normal1440/375, reduced, injected scene-network failure): search empty result/reset, keyboard selection of final task, focused new heading with scrollTop0 after each transition, two required declarations, ready/reset. Reduced mode requests no Spline resource/canvas; network failure retains functional CSS device. WebGL draw-call increments were0 over600ms both visible idle and offscreen.

Concrete defects returned to builder: native canvas watermark overlaps/is hidden by bottom HTML; offscreen desktop?mobile resize then return loses visible model frame. Captures in `scratchpad/qa/spline-independent/{1440-phone,375-phone,offscreen-resize-return}.png`. Native owns fixes and author geometry recheck; do not infer acceptance from ready=true alone.

The initial strict zero-CSP-event assertion failed because the SDK makes one denied JavaScript compilation attempt. `node apps/console/scripts/spline-trace-independent-qa.mjs` traced `new Function` to SDK MessagePack decoder Hc. Source explicitly catches compilation failure, sets optimization threshold Infinity, and uses ordinary key-loop decoding. Actual scene remained rendered with no page/console errors. This is an expected vendor optimization fallback; retain denial of JavaScript unsafe-eval. WASM-only compilation permission is present. Independently injected inline script and onclick handler both remained blocked. No additional remote hosts or missing optional decoder requests observed. Trace evidence `scratchpad/qa/spline-independent/trace.json` includes exact stack/argument and source bundle position. Reduced-motion toggle removes canvas and reloads it successfully with no page errors.

Browser closed and lane released to native for the two concrete fixes. Final rebuilt production-proxy and later deployment acceptance remain pending. Headless WebGL measurements do not certify device frame rate or GPU appearance across physical devices.

Measured intro foreground/background contrast: ink14.03:1, orange5.07:1, blue6.43:1 against the actual rgb(255,252,246) surface. These are decorative brand glyphs; all three also exceed the ordinary text threshold.

Final local release verdict: PASS. After owner fixes and fresh production build, `node apps/console/scripts/spline-final-independent-qa.mjs` exited0. Real1440/375 scene renders with full original canvas attribution below the phone, correct frame around HTML, hidden wallpaper and no overflow. Desktop?offscreen mobile resize?return now restores the frame (302 measured WebGL draw calls for reentry) and then stops (0 additional calls over600ms). Both initial visible idle samples also0. Reduced-motion toggle remains clean. All actual scene/WASM requests200; no page/console errors and only the previously traced caught MessagePack optimization CSP denial. Final screenshots visually inspected: `scratchpad/qa/spline-independent/final/1440-phone-and-attribution.png`, `375-phone-and-attribution.png`, `375-after-offscreen-resize.png`. Browser closed. Both concrete defects are resolved. No broad intro or demo-flow rerun was needed because those handlers/source remained unchanged.

Public acceptance: PASS on deployment `e15d5445-23a8-40fd-a2ca-27b03fed4cea`, after root confirmed Railway SUCCESS. `node apps/console/scripts/white-spline-public-qa.mjs` exited0.1440 normal first visit and ordinary reload displayed the warm-white intro, nine650-weight slots, then removed it and showed the single semantic PLAYER ONE heading. Post-locator observed removal times3082/2941ms from the shuffle start include Playwright polling latency; the precise2300ms+~320ms lifecycle was already measured locally.375 actual supplied scene ready with visible native attribution, correct frame/HTML geometry, all scene/WASM requests200 and no overflow. Actual task?details?two declarations?ready?reset passed. Reduced-motion toggle displayed the CSS fallback and removed the canvas. No page or console errors; no intro CSP violations and only the traced caught SDK MessagePack eval denial for Spline. The JavaScript unsafe-eval prohibition remains intact.

Public evidence: `scratchpad/qa/white-spline-public/results.json`, `1440-white-intro.png`, `1440-film.png`, `375-spline-attribution.png`, `375-reduced-fallback.png`. Intro and phone captures visually inspected. Browser closed. No authentication/API/financial mutations or unrelated regression suite performed.
