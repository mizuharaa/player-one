# PlayerOne — assembly, film, people

Latest owner direction supersedes the scroll-only opening and the restrained introduction/review compositions in the earlier handoff. Keep the successful task demo, typography, product truth, responsive behavior and independent QA workflow. This is an implementation brief, not an acceptance result.

## Opening

The requested hero film must appear automatically. A first eligible visit plays one finite, approximately 2.3-second SVG brand assembly, then lifts the overlay into the film stage with a brief slogan-to-film transition. Do not add another long slogan hold or require a scroll before seeing the film. Subsequent session visits show the film stage directly. Honor restored deep links and browser navigation rather than forcing scroll position.

Use the incumbent React/TypeScript/Vite application and GSAP/SVG. The user's Next.js/React example does not require a framework migration. No paid animation plugins, canvas or WebGL.

Incumbent identity inspection: `public/mark.svg` and `components/identity/Mark.tsx` contain two overlapping lens discs. The old wordmark is HTML text; there is no existing outlined wordmark master. Create an original geometric PlayerOne wordmark retaining that twin-lens vocabulary. Do not use Fauna letter geometry.

The final untransformed SVG is canonical. Actual stems, bowl segments, arches and bars share a single viewBox. Their deterministic starting transforms are computed from their SVG bounding-box centers. Stacked source fragments unfold from two compact lens groups into a brief abstract composition and then assemble the wordmark. No raster logo, SVG text, letter-opacity trick, random trajectories, mid-flight spawning or replacement text fade. The same SVG is used in navigation for a measured transform-only handoff.

Once-per-session is the default, with guarded storage, explicit repeat modes, dev-only replay and timeline inspection. Scroll is locked only for the finite overlay and always restored on skip, teardown, failure or reduced motion. The underlying page is already mounted. Reduced motion skips the assembly without delaying access. Lifecycle and scheduler ownership must preserve the recent ScrollTrigger duplicate-enable fix.

## Visual Story

| Scene | Visual story | Website copy |
| --- | --- | --- |
| Assembly | Twin-lens cluster → abstract separated primitives → geometric wordmark → same navigation logo. | PlayerOne |
| Film | Overlay lifts; supplied opening film fills the viewport automatically. Centered lower-middle heading, supporting sentence, bottom action. | Everyday work. A new perspective. |
| Introduction | Full-width ink/lavender split. Huge white type on ink; a large circular aperture with an illustrative recording viewport on lavender. Frame-corner primitives move with the recording. | Familiar work, recorded differently. |
| Demo | Existing single interactive task-preparation surface over its photographic environment. | Choose work. Prepare Ego. |
| Collectors | Three independent work photographs form an unequal edge-to-edge wall. Coordinated vertical-scroll translations bring its fields into alignment. | Our collectors. / Illustrative scenes; not actual participants. |
| Device | Wearing-position explanation joins the imagery without repeating the whole gallery. | On your forehead. Eyes unobstructed. |
| Review | Strong ink/lime composition containing one large illustrative recording, shaped verdict controls and the human decision explanation. | A person reviews every recording. |
| Payment | Recording-channel strips expand into a broad, readable overlap diagram connected to the review composition. | Approved effective minutes determine payment. |
| Close | Useful questions and actions, then oversized PlayerOne wordmark. | Explore collecting. Sign in to the console. |

## Color and movement

Ink/lavender forms the introduction; ink/lime forms the review peak. Lime is a scene ground, not a text highlighter. Natural photographic color supplies variety. Partner orange/blue stay in the partner identity. Verdict hues remain attached only to verdicts. All new values belong in tokens.

Use frame alignment, aperture movement, assembled geometry and coordinated photographic fields. Avoid disconnected random shapes, decorative gradients/glow, fake live telemetry and ever-running motion timers. Native page scroll remains intact after the brand intro. Reflow rather than shrink on 375/430/768/1024/1440/1920 widths.

The exact unnamed “Our collectors” reference is pending an optional owner link. Use the supplied Flim/Fauna gallery direction now; do not assert an exact reference match without its source.

## Evidence and delivery

Reuse the completed Higgsfield work photographs and responsive POV assets. Existing credits rejected the previous corrective film request; the owner expressly permits reusing suitable assets. Do not purchase credits or submit known-failing duplicates. Three different photographs are sufficient for an honest gallery; never make one film look like a diverse image collection. Keep generated imagery visibly illustrative.

The September 8 hero remains an external-view illustrative film. The September 6 eye-covering film remains unsuitable; any reused device close-up must show unobstructed eyes. No fabricated collectors, identities, metrics, earnings or automatic human-review outcomes.

Implementation ownership: codex_build for page/tokens; logo_assembly for the isolated SVG assembly components; native_ui for outstanding native corrections. Root owns composition and handoff. Claude alone runs independent acceptance and captures assembly phases at quarter speed, header docking, first/repeat/reduced visits, responsive gallery, media failures, focus/scroll restoration and settled idle behavior.
