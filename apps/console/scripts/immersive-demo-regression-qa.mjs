import{withBrowser,newPage}from'./browser.mjs';import{writeFileSync}from'node:fs';import assert from'node:assert/strict';
const out={cases:[],errors:[],mutations:[]};await withBrowser(async b=>{for(const width of[375,1440]){const{page:p,context:c}=await newPage(b,{viewport:{width,height:900}});p.setDefaultTimeout(10000);p.on('pageerror',e=>out.errors.push(String(e)));p.on('request',r=>{if(r.url().includes('/api/')&&r.method()!=='GET')out.mutations.push(new URL(r.url()).pathname);});await p.addInitScript(()=>{localStorage.setItem('playerone.locale','en');localStorage.setItem('playerone:showcase-cookie-choice:v1','declined');});await p.goto('http://127.0.0.1:5190/discover');await p.waitForSelector('[data-film-revealed="true"]');await p.locator('#demo').scrollIntoViewIfNeeded();const tasks=p.locator('.discover-native-task'),row={width};assert.equal(await tasks.count(),3);await p.locator('.discover-demo-search input').fill('QA_NO_MATCH');assert.equal(await tasks.count(),0);await p.locator('.discover-demo-search input').fill('');await p.locator('.discover-demo-filters button').nth(2).click();assert.equal(await tasks.count(),1);await tasks.first().click();assert(await p.locator('.discover-demo-content h3').evaluate(n=>n===document.activeElement));await p.locator('.discover-demo-content > .discover-button').click();assert.equal(await p.locator('.discover-demo-choice input:checked').count(),0);assert(await p.locator('.discover-demo-content > .discover-button').isDisabled());await p.locator('input[name="demo-others"]').last().check();assert(await p.locator('.discover-demo-content > .discover-button').isDisabled());await p.locator('input[name="demo-sensitive"]').last().check();await p.locator('.discover-demo-content > .discover-button').click();row.physicalInstruction=await p.locator('.discover-demo-content .discover-demo-task-name').innerText();await p.locator('.discover-demo-content > .discover-button').click();assert.equal(await tasks.count(),3);assert.equal(await p.locator('.discover-demo-search input').inputValue(),'');assert.equal(await p.locator('.discover-demo-filters button').first().getAttribute('aria-pressed'),'true');row.searchFilterDetailsDeclarationsReset=true;out.cases.push(row);await c.close();}});assert.equal(out.errors.length,0);assert.equal(out.mutations.length,0);writeFileSync('scratchpad/qa/immersive-media/demo.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));
