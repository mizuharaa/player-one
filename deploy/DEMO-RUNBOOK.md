# Thursday demo runbook — staff-assisted live demonstration

For the stakeholder meeting on **Thursday 2026-09-17**. This is the file the
room is run from.

It **supersedes steps 1–10 and the payment runbook of
[`centre/DEMO-SCRIPT.md`](centre/DEMO-SCRIPT.md)**, which was written for a
centre-PC-only walkthrough with no timing, no named owner per step and no
recovery. That file keeps the long-form
**[card procedure](centre/DEMO-SCRIPT.md#card-procedure--exfat-auto-mounted-measured)**,
and this one does not repeat it — but read the next paragraph first, because
the measured card is not the card that procedure was written for.

**The card, measured (not inferred).** The real TF card is **exFAT**, label
`PlayerOne`, 240 GB. It automounts: a drive letter on Windows,
`/media/<user>/PlayerOne` on Linux. So **there is no WSL step, no `usbipd`
bind/attach, and no ext4 `ro,noload` mount** — the whole `usbipd`/ext4
procedure in `DEMO-SCRIPT.md` was written from documentation for an ext4 card
and does not apply to this one; DEMO-SCRIPT.md now records that route as void
and holds the measured exFAT procedure instead. What applies on Thursday: insert the
reader, let the card mount, and run the card-intake command (being written on
`lane/card-path`) against the session directory on the card. It copies with
checksums first and imports the copy. **The card is never written to and never
cleared** — CLAUDE.md rule 6, no exceptions, in no recovery path.

## What is being claimed, and what is not

Say this, in these words, before anything is shown:

> This is a staff-assisted live demonstration of the whole loop on the real
> platform. Everything you will see is real: real footage off a real camera
> card, a real upload, a real human review, a real bill computed by the server.
> Three things are not ready and we will point at each one when we reach it:
> collectors cannot yet receive their own sign-in codes, so staff sign in for
> them; the remote reviewer seat is disabled until Legal signs the playback
> architecture, so a VNG operator inside Vietnam does the review today; and no
> payment can be recorded, because
> ZaloPay has not confirmed the destination account. That last one is the
> platform refusing to send money to an unconfirmed account, and it is
> deliberate.

Not claimable on Thursday: pilot-ready, collector self-service sign-in, a
completed payment.

## The cast — five roles, five people, and they do not swap

| Role | Who | What they hold |
|---|---|---|
| **narrator** | — | says the framing above and the honest lines; presses nothing |
| **operator** | — | the console, signed in as `op-1` (administrator) + the machine token; holds the card |
| **finance** | — | a *second* browser profile, signed in as `fin-1`; never signs in as `op-1` |
| **reviewer** | — | a *third* browser profile, signed in as `rev-1`; no centre. On the default build this seat can sign in but **cannot claim or judge** (HTTP 451) — see [0:19](#019--human-review--operator-variant-a-or-reviewer-variant-b). Keep the profile: signing in is itself worth showing. |
| **collector-phone** | — | the Android handset with the v3 collector app, signed in as `+84900000001`. **The app opens in English** (`DEFAULT_LOCALE = 'en'`, `apps/collector/src/i18n.ts`); Vietnamese is a tap away and every phone beat below quotes both |

Separation of duty is not a presentation device: `settle_generate_by_finance`
refuses a bill generated by a finance actor, and `payout_separation_of_duty`
refuses a payment by whoever declared the account. If one person signs in as
both, a step in this script will be refused and the room will read it as a
fault.

Fill in the names before Wednesday's rehearsal. An unnamed row is an unowned
step.

## T-60 minutes — preflight, before anybody is in the room

```bash
DATABASE_URL="postgres://playerone_app:...@<db host>:5432/po_demo_thursday" \
PLAYERONE_PUBLIC_URL="https://console.<domain>.vn" \
STORAGE_ENDPOINT=... STORAGE_BUCKET=... STORAGE_KEY=... STORAGE_SECRET=... \
PLAYERONE_BACKUP_DIR=/data/backups \
node deploy/demo-preflight.mjs
```

Take the dump **before** running it, or the `backup` check has nothing to find:

```bash
docker compose exec postgres pg_dump --format=custom -U postgres \
  -d po_demo_thursday > /data/backups/po_demo_thursday-$(date +%Y%m%d-%H%M).dump
```

Every line must read `PASS` or `SKIP`. It is read-only — SELECTs, one GET, one
TLS handshake, one HEAD, one directory stat — so it can be re-run as often as
you like, including during the demo.

**Two variants, and it matters which.** `/healthz` is **not an API route**:
`serve.ts` answers it 404, and the route belongs to `deploy/http-server.mjs`
and the cloud Caddyfile, which rewrite it to `/whoami`. On the cloud variant
the front server and a fresh dump are both required, so either one missing is a
`FAIL`. On the centre-PC LAN variant neither exists by design, and both checks
report **`SKIP` with the reason** instead. The variant is taken from
`PLAYERONE_DEMO_VARIANT=lan|cloud`, or inferred from the origin — plain HTTP
means LAN. A `SKIP` never fails the run; a `FAIL` always does.

A `FAIL` on `certificate-expiry`, `healthz` or `storage` is a **cloud-loss
decision point**, not something to fix at T-10. Go to
[Fallback A](#fallback-a--cloud-loss-the-centre-pc-variant).

If the database is empty, seed it once:

```bash
DATABASE_URL="postgres://playerone_app:...@<db host>:5432/po_demo_thursday" \
PLAYERONE_DEMO_MACHINE_SECRET=... PLAYERONE_DEMO_ADMIN_SECRET=... \
PLAYERONE_DEMO_FINANCE_SECRET=... PLAYERONE_DEMO_REVIEWER_SECRET=... \
node packages/api/scripts/seed-stakeholder.mjs
```

Add `PLAYERONE_DEMO_DEVICE_SERIAL=<the unit that will record>` if it is not the
default `AZER76400HV`. **This one matters on the day:** the handover's device
and the recording's basename have to agree, and on 2026-09-14 they did not —
the seed bound `EGO-DEMO-0001`, the unit writes
`Orbbec_Ego_AZER76400HV_*`, and every episode off that card carried the
`SERIAL-CONFLICT` defect. A second unit is added the same way, with its own
serial; changing the serial against an already-seeded database is refused
rather than rewritten, so it means a fresh demo database.

It refuses any database whose name does not start `po_demo` or
`playerone_demo`, it truncates nothing, and a second run adds nothing. Pass the
four secrets from the environment so a rerun is a rerun; omit them and it
generates and prints them once, and a later run with different values is
refused rather than overwriting a sign-in.

## The script — 35 minutes

Each row: the clock, who acts, what they do, the **state word** that must
appear, and what to do when it does not. The state words are the platform's
own (`batch_status`, `verification_state`, `review_state`,
`settlement_state`, and the app's `status`), not labels invented for the room.

### 0:00 — framing · narrator

Read "What is being claimed" above. No screen. **Recovery:** none needed; if
the room asks for the brief, `docs/` has it, do not improvise scope.

### 0:03 — card in, session declared · operator

Insert the reader; the exFAT card mounts by itself (a drive letter on Windows,
`/media/<user>/PlayerOne` on Linux). Run the card-intake command against the
session directory **on the card** — it checksums, copies, re-checksums, and
imports the copy, never the card. Do the copy before the room if the session is
large; the import is what the room watches. In the console: **Counter → new
handover**, pick the collector, the device and the card id, declare the
session.

The exact command, and it is exact for a reason — without `--task` it is
refused `session_claim_missing` (measured 2026-09-14: the default is the first
published task, "Một buổi làm việc", and the demo collector's only live claim is
"Demo housework"):

```bash
export PLAYERONE_MACHINE_IDENTIFIER=demo-machine-1
export PLAYERONE_MACHINE_SECRET=...      # the seed printed these four
export PLAYERONE_OPERATOR_REF=op-1
export PLAYERONE_OPERATOR_SECRET=...
export PLAYERONE_MEDIA_ROOT=/data/media  # the API's own media root

node packages/api/scripts/card-intake.mjs \
  "/media/<user>/PlayerOne/Orbbec_Ego_AZER76400HV_<stamp>" \
  --card TF-DEMO-0001 \
  --collector +84900000001 \
  --task "Demo housework" \
  --others-in-frame no --sensitive no
```

`--task` takes a name or a uuid. The two declarations have no default on
purpose: they are the APP-17b answers the collector gave at the counter, and a
default would be filing a consent answer nobody made. `bin/counter.ts import`
is the older path and takes **uuids** for `--task`, `--collector`, `--device`
and `--scenario`; `card-intake.mjs` derives the handover, batch and session ids
from the centre, the card, the collector and the day, so running it twice
replays into the same rows instead of opening a second batch.

**Expected:** a `handover_id` and a session on screen; the batch reads
**`importing`**. Say the batch id aloud.

**Recovery:** re-run the same `card-intake.mjs` line from 0:03; it is
idempotent (measured: the second run prints `duplicate`, reuses the handover,
batch and session, and creates nothing). `counter.ts import` is not a
substitute: it wants five uuids, not a phone and a card. If the resolver answers
**`operator_confirmation_required`**, that is the platform refusing to guess
between two handover-origin sessions: confirm the session on the `/episodes`
screen and say why it asked.

### 0:08 — upload and cloud verify · operator

The `card-intake.mjs` run from 0:03 already uploaded the batch and read it
back; `counter.ts upload --batch <id>` resumes an upload that stopped.

**Expected:** `cloud_verified: true` on stdout, the episode reads
**`verified`**, and there is one `cloud_verifications` row **per file, each with
its own sha256**. Read one hash aloud. Do not accept a run that only shows
`cloud_verified_at`: the proof is bytes re-read and re-hashed inside the VPC,
not an ETag.

**Recovery:** a failed verify leaves the batch resumable — re-run
`counter.ts upload --batch <id>`; it is idempotent and makes no second episode.
If the bucket itself is gone, go to
[Fallback A](#fallback-a--cloud-loss-the-centre-pc-variant). **The card is
never cleared, in any recovery, for any reason** (CLAUDE.md rule 6).

### Broken recording beat — shown ONLY if announced first

A card can carry a session the platform will not send to a human. The owner's
two recordings on 2026-09-14 were exactly that: the left camera wrote a
24-byte file, so both sessions carry `MEDIA-UNREADABLE` at severity
`quarantine`, plus `PTS-EMPTY`, `STREAM-SKEW-HIGH` (≈3998 ms) and, until the
seed's serial was fixed, `SERIAL-CONFLICT`. The ingest state is `quarantined`,
the episode resolves to no session, and the review queue cannot offer it:
`eligible` needs `resolution_state = 'resolved'` **and** a non-quarantined
ingest, and it fails both. Verified: `GET /api/review/next` answers 204.

**Announce it before it appears, in these words:** "one camera on this unit
failed — watch what the platform does with it." Then the quarantine reads as
the safety catch it is: the bytes were copied off the card with matching
checksums, uploaded, re-read in storage and confirmed, and then the platform
refused to ask a person to judge a recording with a dead camera, and refused to
pay anybody for it. The card was never written to and never cleared; both
recordings are still on it, whole. Unannounced, the same screen reads as a
broken demo.

**Where the operator finds them:** console **Episodes → "Episodes needing
attention"** (`/episodes`), panel **"Blocking, this batch"**, Needs column **"A
session to attribute it to"** — from `GET /upload-batches/:id/exceptions`. Not
`/episodes/stuck`, which is parked/held only and answers with an empty list.

**Two things the narrator must expect on that screen.** First, the browse rows
show **blank task, collector and device columns** — a quarantined episode is
attached to no session, so there is nothing to name there; it is not a rendering
fault. Second, **the defect sentences are not on that screen**: the `bo.flag.*`
sentences ("A container exists but cannot be decoded.") render in `Review.tsx`
only, and these episodes can never reach a review screen. They live in
`episode_defects` and in the intake command's own stderr, which is where to
read them from if the room asks why.

**The paid path needs a different recording.** Use a session with both cameras
— the corpus session `ego_AZER76400FE_20260813_072310` went the whole way
(partial verdict, 5.0 s payable of 8.5 s measured, a 99.9996 VND line) — or,
better, a fresh good recording made before Thursday, because it is the owner's
own footage.

### 0:14 — the handset · collector-phone

**Read this before the rehearsal.** The app is the v3 build and every screen
name below is its own. The previous runbook said "Uploads → Tải lên → Chọn thư
mục phiên"; none of those three labels is on screen any more. The dock is
**five icons with no text**, so each one is named here by the accessibility
label a screen reader announces — that label is the only name it has:

| Dock icon (English) | Vietnamese | Screen it opens |
|---|---|---|
| Home | Trang chủ | the greeting, this cycle's money, the next step |
| Explore | Khám phá | the tasks on offer, photo-led (was "Nhiệm vụ") |
| Sessions | Tải lên | the episode list and the upload panel (was "Tải lên") |
| Income | Thu nhập | per-episode money, the destination, the timeline |
| Profile | Hồ sơ | account, language, notifications, log out |

Profile's icon is **not a glyph** — it is the collector's initials on a plum
disc, `DC` for `Demo Collector`. Do not describe it as a person icon.

**Language, if the room asks.** **Profile → Settings → Language** is the
explicit picker and lists each language in its own words: `Tiếng Việt`,
`English`, `中文`. Home also carries a chip whose label *is* the language it
switches to — on the English app it reads `中文`, because the cycle is
vi → en → zh. Say which language is on screen before reading a line aloud.

**Sign in.** The landing carries three lines over the video — "Wear it." /
"Live your day." / "Reviewed minutes, paid." (VI: "Đeo camera." / "Sống như mọi
ngày." / "Phút nào được duyệt, phút đó có tiền.") — then **Sign in** (Đăng
nhập). Type `+84900000001`, press **Send code** (Gửi mã). The server fills the
code in itself and the app says so: **"The server is in demonstration mode and
filled the code in."** (VI: "Bản trình diễn tự điền sẵn mã.") The narrator says
here that the six-digit code was filled in by the demo account because ZNS is
not issued yet — **staff-assisted, and a real collector could not do this
today.**

**First install only: the three intro cards.** On the first arrival at Home the
app shows Onboarding — three swipeable cards, **"FIND WORK NEAR YOU"**, **"THE
CAMERA RECORDS. YOU JUST WEAR IT."**, **"FOLLOW YOUR REVIEW"** (VI: "TÌM VIỆC
GẦN BẠN", "MÁY TỰ GHI. BẠN CHỈ CẦN ĐEO.", "THEO DÕI KẾT QUẢ"). **Skip** (Bỏ
qua) is top-right on every card; the last card's button is **"Show me around"**
(Dẫn tôi đi một vòng), which starts the coach-mark tour.

The second card is the honest one and is worth reading aloud: *"Put the Ego on
and start it with the buttons on the camera. This app never starts or stops a
recording."*

**It appears once per install.** The flag lives in the keystore
(`playerone.collector.guideOffered`, `apps/collector/src/guide/seen.ts`), so a
handset you rehearsed on **will not show Onboarding again**. If the room is to
see it, reinstall the APK on Wednesday and do not open the app again until
Thursday. If it does not appear, do not hunt for it in the room — say the
handset has been used before and move on.

**Prepare, and the two APP-17b declarations.** Home's primary button is
**Prepare** (Chuẩn bị). It opens the reading step — **"Before each recording
shift"** (Trước mỗi ca ghi hình), whose own text says the button below "only
opens session preparation; it does not record check results or turn on the
camera" — and then **"Continue to session preparation"** (Tiếp tục chuẩn bị
phiên) reaches the five-step form. One step per screen, with a progress bar:

1. **Task** (Nhiệm vụ) — the claimed task, `Demo housework`
2. **Scenario** (Bối cảnh) — "Choose where you will carry out this collection
   session." At home / Office / Shop / Warehouse
3. **Device** (Thiết bị) — `AZER76400HV`
4. **"Might other people appear in frame?"** (Có thể có người khác trong khung
   hình?) — **Yes** / **No**
5. **"Might sensitive information appear?"** (Có thể xuất hiện thông tin nhạy
   cảm?) — **Yes** / **No**, then **Create session** (Tạo phiên)

Those last two are APP-17b and they are the whole of the collector's consent —
there is no third declaration and none is to be invented. **Neither has a
default**: leave one unanswered and the app says "Answer both declarations."
(Bạn cần trả lời cả hai câu) and the button stays disabled. Say that: a
pre-ticked box would be filing a consent answer nobody made.

The confirmation screen reads **"Session created"** (Đã tạo phiên) with
**Session ID**, **Task** and **Device**, and the note **"The app never starts
or stops recording. The record control is on the device, in your hands."**
(Ứng dụng không bật và không tắt máy ghi. Nút ghi nằm trên thiết bị, trong tay
bạn.) There is no record button on this screen or any other, and the room
should be told that rather than left to look for one.

**This beat writes a real row.** Completing it creates a genuine
`collection_sessions` record, `session_origin = 'app'`. The money in this demo
runs through the **handover** session the operator declared at 0:03, not this
one. Either walk the five steps and stop at step 5 without pressing **Create
session**, or press it and say plainly that a second session now exists and
which one the bill came from. Decide which at Wednesday's rehearsal, not in the
room.

**The upload.** **Sessions** (the third dock icon) is the episode list. Its
header carries **"Upload a recorded session"** (Tải lên phiên đã ghi), and that
button opens the panel:

1. **The choice, and it is the honest half of this demo.** Two options:
   **"Send from this phone"** (Gửi từ điện thoại) or **"Hand the card to the
   operator"** (Bàn giao thẻ cho nhân viên). Picking the card option shows the
   handover guidance and the panel's button becomes **Done** — the app has
   nothing more to do, because the card goes to a person. That is Path C, it is
   what the pilot actually does, and it is the same route the operator ran at
   0:03. Pick **"Send from this phone"** to carry on.
2. **Choose session folder** (Chọn thư mục phiên) — the system folder picker.
   The panel explains first: "Pick the session folder on this phone. The server
   checks and measures it, not the phone."
3. **"Choose the collection session this recording belongs to."** (Chọn phiên
   ghi hình của bản ghi này.) Each row is `<Scenario> · <date>`, e.g.
   `At home · 2026-09-15`. With no declared session the panel says "You have
   not declared a collection session yet. Create one before uploading." — which
   is why Prepare comes first.
4. **The confirmation.** The screen is headed **"Confirm upload?"** (Xác nhận
   tải lên?) and states the rule: **"Data leaves your phone only when you
   confirm it. Never automatically, never silently."** (Dữ liệu chỉ rời máy khi
   bạn bấm xác nhận. Không tự động, không âm thầm.) It shows **Folder** and
   **Files** (the count), and the session, each with a **Change** link. Read
   that sentence aloud — there is no upload-all button anywhere in the app, by
   design.
5. **Start upload** (Bắt đầu tải lên). Two progress bars in order, **"Hashing
   files"** (Đang kiểm tra tệp) then **"Sending files"** (Đang gửi tệp), each
   counting files done over files total.

**Expected:** the panel ends on **"Received and measured"** (Đã nhận và đo
xong) — the `ingested` delivery state — and a toast says the same. The episode
then appears in the **Sessions** list reading **"Uploaded"** (Đã tải lên), the
app's word for APP-23's `uploaded`, and the same episode appears in the
operator's console. Tap the row for **Size** in GB; a session the server sent
no size for reads **"Not supplied"** (Chưa rõ) and never a measured zero.

The list's filter chips are **All tasks** plus the six state words — Pending
upload, Uploading, Uploaded, Under review, Review passed, Review failed. The
search field is **"Search sessions or status"**.

**If the room asks to see the work on offer.** **Explore** lists four tasks,
each a photograph with the rate burned into the corner — `1.200 ₫` and
**"VND/effective minute"** (đ/phút hiệu quả) for `Demo housework`, up to
`6.000 ₫` for `Ca kho hàng`. The two chips on each card are the scenario word
and the claim state (**Claimable** / **Already claimed** / **At capacity**);
they are derived from the task, not a tag column. **Every photograph is a
stand-in** and each card says so — "Illustrative setting photograph" (Ảnh minh
họa bối cảnh) — because `tasks` has no image column and the still is chosen
from the task's `type`.

**Expect three empty sections on a task's detail screen**, and they are not a
seeding fault: **Instructions**, **Payment rule** and **Where you record** all
read **"PaXini has not supplied this content yet."** (PaXini chưa gửi nội dung
này.) APP-09 is not built server-side — `tasks` has no instructions, privacy
notice or payment rule column at all (measured on `po_demo_v3`, 2026-09-15), so
no seed can fill them and the app refuses to invent prose a collector would
read as the real instructions. **Task rates** above them is real: Target `240
minutes`, Minutes taken so far `81 minutes`, Places left `4`.

**Recovery:** [Fallback B](#fallback-b--handset-upload-failure-the-debug-delivery-page).
If the upload itself is refused the panel prints the server's own reason in the
collector's language — a checksum mismatch, a name collision, an unrecognised
folder name — so read the sentence on screen rather than guessing. If the app
cannot reach the server at all: **Profile › About › Server** → type the
laptop's origin, **Save**; the app signs you out and returns to Landing.

### 0:19 — human review · operator (variant A) or reviewer (variant B)

**Read this before the rehearsal: the reviewer seat is disabled on the default
build, and not merely deprived of video.** With `PLAYERONE_REVIEWER_MEDIA=0` a
`role = 'reviewer'` session is refused **on claim and on verdict**, HTTP 451
`playback_unauthorised`, detail `D11 / Part 7.3` (`review.ts`, asserted by
`packages/api/test/reviewer.test.ts` around line 543). Measured 2026-09-14:
`rev-1` signs in fine — at `POST /api/session` with `role: reviewer`, **not**
`/auth/operator`, which excludes reviewers by design — then `GET
/api/review/next` answers 204 and `POST /api/review/claim` answers 451. There is no residency field and no in-country
marker to set — one flag decides it, and the operator role is exempt because a
VNG counter operator is inside Vietnam at the machine holding the files.

So there are two honest variants. **A is the default.**

**Variant A — reviewer media off (Legal has not signed).** The **operator**
profile performs the review: signs in with both tokens, opens `/review`, claims
the episode and decides it. The narrator says:

> The reviewer seat is disabled on this build until Legal signs the playback
> architecture. Remote playback of raw Vietnamese footage is not authorised, and
> the platform refuses a remote reviewer's claim outright rather than showing
> them a verdict button they cannot honestly press. A reviewer in Shenzhen can
> sign in and can see nothing to judge. Today a VNG operator, inside Vietnam, at
> the machine holding the files, does the review — which is the arrangement the
> pilot was designed for and what §7.2 moves to permanently.

**Variant B — reviewer media on (an owner decision, not a workaround).**
`PLAYERONE_REVIEWER_MEDIA=1` in the demo environment, which also requires
`PLAYERONE_SECURE_COOKIES=1` (the service refuses to boot otherwise, so it
cannot be done on the plain-HTTP LAN variant). The `rev-1` profile then claims
and judges with playback, and the narrator says that remote playback is switched
on deliberately for this demonstration. **Do not switch to B in the room, and do
not switch to it to rescue a failed step.** It is a decision taken before
Wednesday's rehearsal or not at all.

Whichever variant, the acting reviewer opens `/review`, claims the episode, and
decides it.

**Expected:** the review reads **`pass`**, with `measured_duration_s` and
`effective_duration_s` both on screen. Read both aloud: the gap is the answer
to "why isn't the whole clip paid" — payable time is the *intersection* of
stream coverage, not the union.

**Recovery:** if the queue is empty, the episode is not eligible — a quarantined
ingest, a zero measurement, or a defect whose catalogue entry says
`blocks_review` (two of the five real sample sessions carry `MEDIA-TRUNCATED`
and cannot be reviewed at all). Say which, from the `/episodes` screen, and
review the seeded episode instead. Do **not** flip
`PLAYERONE_REVIEWER_MEDIA=1` in the room to "make review work" — that is
variant B, it is a decision taken beforehand, and variant A needs no flag at
all.

### 0:24 — settlement and the bill · operator (NOT finance)

The verdict already wrote the settlement. The operator then runs the cycle:
`POST /api/settle/bills`, the `/settle` screen.

**Expected:** the settlement reads **`pending_settlement`** and then
**`bill_generated`**; a `bills` row with a `total` that is the exact sum of its
lines, never rounded.

**Recovery:** a refusal named `settle_generate_by_finance` means finance is
signed in on this screen — swap to the operator profile; that refusal is the
separation of duty working. A bill worth under one dong is refused as
`payout_attempts_amount_positive_check`; use the seeded bill.

### 0:28 — the payment, and the honest ending · finance

Finance opens `/settle/bills/<bill id>` and tries `POST
/api/payout/bills/:id/mark-paid` with the bill's whole-dong total.

**Expected — and this is the ending, not a failure:** the destination reads
**`unverified`**, and the call is refused with HTTP 409 and the constraint name
**`payout_attempts_account_unverified`**. Say that name, because it is the one the API
sends (measured 2026-09-14:
`{"error":"refused","constraint":"payout_attempts_account_unverified"}`). There are two
gates with two names and the room only ever sees the first: the application
gate `payout_attempts_account_unverified` (`payout/routes/payout.ts`) fires before the
insert, and the database trigger `payout_attempts_account_unverified`
(`payout_attempts_guard`, migration `0012`, replayed in `0016`) is the second
one behind it, which is what makes the refusal impossible to drift past in
application logic. After the refusal: `payout_attempts` has **0 rows**, the
settlement is still `bill_generated`, the account is still `unverified`.
Nothing moved.

**The line the narrator says, verbatim:**

> This bill is correct and ready. It shows as **awaiting payment — destination
> unverified**. ZaloPay has not confirmed that this wallet belongs to this
> person, and the platform will not send money to an unconfirmed destination.
> That is a deliberate gate, not a missing feature. The moment VNG issues the
> ZaloPay verification credential, this same screen verifies the account and
> the same button records the payment.

**What changes if a verification credential does arrive before Thursday:** all
four of `PLAYERONE_ZALOPAY_APP_ID`, `PLAYERONE_ZALOPAY_PAYMENT_ID`,
`PLAYERONE_ZALOPAY_KEY1` and `PLAYERONE_ZALOPAY_PUBLIC_KEY` have to be present
— without all four no account can leave `unverified`. Then, and only then:

- Verification runs **on an isolated sandbox database**, never this demo
  database and never anything named live, with `PLAYERONE_ZALOPAY_ENV=sandbox`.
  A sandbox credential verifies sandbox wallets only.
- `PLAYERONE_PAYOUT_MODE` stays **`manual`**. A person sends the money and types
  the reference; the rail is not switched on for a demonstration.
- Re-run the preflight: `seed-payout` will read `verified` and say so, which is
  the signal to use the verified ending instead of this one.
- The verified ending is: finance retypes the exact whole-dong `amount_vnd` and
  the real transfer reference, `payout_attempts` records `amount_vnd`,
  `manual_reference` and `settled_at`, and the settlement reads
  **`manually_paid`**.

If no credential arrives, **do not simulate a payment.** A stubbed payment
shown to stakeholders is the one thing in this demo that would be a lie.

### 0:31 — the collector sees the same thing · collector-phone

Open **Income** — the fourth dock icon — and pull down to refresh. Every figure
below came off `GET /api/me/income` and `GET /api/me/payout` on `po_demo_v3`,
measured 2026-09-15; the seed's own numbers, not invented ones.

**The header.** **"This cycle · 01/09 – 15/09"** (Thu nhập kỳ này), then the
confirmed total **`64.800 ₫`** in large type, the word **"Confirmed"** (Đã xác
nhận) under it, and **"Including estimates: 97.200 ₫"** (Kể cả ước tính). The
app computes none of these — all four strings are the server's, and the cycle
figures must match the console's.

**Four round buttons** under the header: **Sessions**, **Cycle statement**,
**Where you get paid**, **Help**. Two of them carry this beat.

**The awaiting row, and this is the ending the room is here for.** Tap **"Where
you get paid"** (Nơi nhận tiền). It reads:

> **Awaiting verification** — with a `?` mark, never a tick
> **ZaloPay wallet · •••• 0001**

That is the app's own rendering of `status: "awaiting"` from
`GET /api/me/payout`, masked by the *server* to the last four digits. Say the
words on screen: the collector is told where the money will go and that the
destination is not yet confirmed. **A verified destination would carry a ✓ and
the word "Verified"** — nothing in this demo has ever produced that, and no
fixture may seed it.

**The transaction rows.** Five episodes, each with its own settlement word. The
two on the bill the operator generated read **"Needs something from you"** (Cần
bạn xử lý) at `21.600 ₫` and `43.200 ₫`. Read that honestly rather than
literally — the server's own longer sentence behind it is *"We cannot pay this
yet. Your ZaloPay payout account is missing or not verified."* **It is the
platform, not the collector, that has something to do here**, and the narrator
should say so: VNG owes the ZaloPay verification credential. Do not let the row
read as the collector having forgotten a step.

The other three: **"Approved, awaiting a bill"** (`32.400 ₫`, the estimate
half of the cycle), **"Uploaded, awaiting review"** (`—`, nothing to pay yet),
and **"Not accepted"** (`0 ₫`, the failed review). Each row carries a tag,
**Confirmed** with a ✓ or **Estimated** with a `~`: a reviewer's figure and a
guess are never drawn alike.

**The settlement timeline.** Tap any row, then the **Updates** (Cập nhật) tab.
Three steps, and a step is bold only once it has happened:

| Step | On a billed episode | Why |
|---|---|---|
| **Uploaded** (Đã tải lên) | done | the server has a state for it |
| **Reviewed** (Đã duyệt) | done | a human decided; `confirmed` is that word |
| **Paid** (Đã chi trả) | **not** done | no payment exists, and none is faked |

The **Paid** step carries the settlement word as its note, so the timeline ends
on the same honest state the console ended on at 0:28. Nothing here infers a
step from a figure being present — an estimate has an amount too, and drawing
that as reviewed would be the app telling a collector they had been paid.

The **Details** (Chi tiết) tab beside it shows **Effective minutes** and
**Settlement** — `18` and `36` on the two billed episodes, which are the same
effective minutes the reviewer's verdict produced at 0:19. **Cycle statement**
breaks the header into Confirmed `64.800 ₫`, Estimated `32.400 ₫`, Including
estimates `97.200 ₫`.

**The notifications screen, and it is deliberately empty.** **Profile →
Settings → Notifications.** The first line is the honest one:
**"Notifications are not connected yet. Check Sessions and Income for current
status."** (Thông báo chưa được kết nối. Xem Phiên và Thu nhập để biết trạng
thái hiện tại.) Below it an illustration, **"Nothing yet"** (Chưa có gì) and
**"Review results, payments and session reminders show up here."** (Kết quả
duyệt, thanh toán và nhắc buổi ghi sẽ hiện ở đây.)

**This is not a seeding gap and there is nothing to fix.** No push transport
exists, so the inbox is structurally empty on every real handset; the settings
below it list the three groups — Review results, Payments, Session reminders —
with both switches **disabled**, and say why: "Email and push settings are
unavailable until notifications are connected." Announce it before opening the
screen, the same way the broken-recording beat is announced, or an empty inbox
reads as a broken demo. **Do not try to seed a notification** — there is no
table behind this screen.

**Recovery:** if "Where you get paid" reads **"Not set — contact a support
point"** (Chưa khai báo — liên hệ điểm hỗ trợ), the account was declared
against a different collector id — check `seed-payout` in the preflight output,
which prints the `verify_status` it found. If it reads "We do not yet know where
to pay you", the server sent a status this build does not know, which is a
server mismatch and not a phone fault. Either way, pull the server's own answer
with the console instead of retyping it from the phone.

### 0:33 — the evidence bundle · operator

```bash
DATABASE_URL=... node packages/api/scripts/demo-evidence.mjs --bill <bill id>
```

**Expected:** one JSON holding every row from every step above, the file
digests, both durations, the unit price, the exact bill total, and the running
server's own `git rev-parse HEAD` — so the bundle names the exact code that
produced what the room just watched. Offer it to the stakeholders; it is the
artefact they can take away.

### 0:35 — close · narrator

Name the three gaps again (sign-in codes, reviewer media, payment
verification), name who owns each, and stop. Do not take scope decisions in the
room.

---

## Fallback A — cloud loss: the centre-PC variant

Triggered by: `healthz`, `certificate-expiry` or `storage` failing the
preflight; the VM unreachable; or the GreenNode bucket refusing a HEAD.

**What the operator types.** The origin changes and nothing else does. On the
centre PC (`deploy/centre/README.md`):

```powershell
node deploy/centre/check.mjs preflight deploy/centre/centre.env   # must exit 0
.\deploy\centre\run-api.cmd
.\deploy\centre\run-caddy.cmd
node deploy/centre/check.mjs health deploy/centre/centre.env      # must exit 0
```

Then, on the LAN origin the centre PC serves (`PLAYERONE_PUBLIC_URL`, a private
IPv4 over plain HTTP, `PLAYERONE_SECURE_COOKIES=0`):

- **console and finance/reviewer profiles:** open that origin instead of the
  cloud hostname. Sign-ins are the same rows; the seed is per database, so the
  centre database needs its own `seed-stakeholder.mjs` run.
- **the handset:** the app's origin is **baked into the APK**
  (`EXPO_PUBLIC_API_URL`), so a handset built for the cloud origin cannot be
  repointed in the room. Either the LAN-origin build is already installed
  (build it Wednesday, keep both APKs) or the handset step becomes
  [Fallback B](#fallback-b--handset-upload-failure-the-debug-delivery-page).
- **the narrator says:** "we are showing this on the upload centre's own
  machine today; the cloud leg is deferred" — and does not claim the cloud leg
  was demonstrated.

**Acceptance gates for the centre-PC variant.** All five, or it is not a
demo-ready fallback:

- [ ] **1. Same revision.** The centre PC runs the same deployed revision as
      the cloud did. There is no SHA on any HTTP route — `/healthz` answers
      `{"ready":true}` and `/api/engineering/status` reports security and
      service state, neither carries a revision — so take it from the checkout
      the server runs from: `git -C <checkout> rev-parse HEAD` on the centre PC,
      and the same on the VM (or the deployed image id). The evidence bundle
      from step 0:33 records the same value, which is how the bundle names the
      code that produced it. The two must match. Rehearsed on ______ by
      ______: PASS / FAIL
- [ ] **2. Card intake passes on it**, with the P0-2 evidence list: real session
      id, source and copy checksums matching, `counter.ts import`, upload,
      verify, review, bill id and amount, then the import and upload retried
      once with **no duplicate episode and no second bill**. Rehearsed on
      ______ by ______: PASS / FAIL
- [ ] **3. Phone upload passes on it** against the LAN origin, with the P0-3
      evidence list: sign in, a distinct real recording uploaded over the demo
      network, `ingested` in that console, income and payout read back from the
      server after an app restart. Rehearsed on ______ by ______: PASS / FAIL
- [ ] **4. `demo-preflight.mjs` passes against the centre database** — the same
      command as T-60, with the centre's `DATABASE_URL` and LAN
      `PLAYERONE_PUBLIC_URL` (the certificate check then reads PASS with "no
      certificate to expire", which is correct for a LAN origin and must not be
      read as TLS coverage). Rehearsed on ______ by ______: PASS / FAIL
- [ ] **5. One uninterrupted rehearsal** of the centre-PC variant, start to
      finish, no fixes mid-run. Rehearsed on ______ by ______: PASS / FAIL

## Fallback B — handset upload failure: the debug-delivery page

Triggered by: the phone cannot reach the origin, the upload stalls, or the app
crashes.

The console's **Engineering → Debug delivery** page
(`/engineering/debug-delivery`) pushes a session folder from the operator's own
browser through the same delivery code the phone uses. It is available only
where the server reports `PLAYERONE_DEBUG_DELIVERY=1`, and it needs the bucket's
CORS rule for this console origin
(`node packages/api/scripts/bucket-cors.mjs https://<console origin>`) — without
it the page fails with `TypeError: Failed to fetch`, no status and nothing in
the API log, because the request never leaves the browser. Apply the rule and
prove the page **before** Thursday.

**The narrator says:** "the handset could not reach us, so an operator is
pushing the same recording through the same upload path from the console. This
is the platform's own debug tool and it is standing in for the phone." Do not
let it be read as the phone working.

`PLAYERONE_DEBUG_DELIVERY=1` is refused by `deploy/centre/check.mjs` in
`https-production` mode on purpose. On a demo deployment it is a deliberate,
temporary choice; remove it afterwards.

## Fallback C — review blocked: metadata-only review, stated honestly

Triggered by: the queue is empty, or somebody asks the reviewer to play the
video.

There is nothing to switch on in the room. `PLAYERONE_REVIEWER_MEDIA=0` refuses
a reviewer's **claim and verdict** outright, HTTP 451 `playback_unauthorised` —
not a reviewer working without pictures — and that is the deployed, intended
state. Variant A (the operator reviews) is the answer; see
[0:19](#019--human-review--operator-variant-a-or-reviewer-variant-b). If the queue is empty, name the reason
from `/episodes` (quarantined ingest, zero measurement, or a `blocks_review`
defect) and review the seeded episode instead. If the room asks to see the
footage, the operator can play it **locally on the centre PC**, where media
never leaves Vietnam — say that is what is happening and why it is not what the
reviewer sees.

---

## Recovery rehearsal — each one run once, before Thursday

Described is not rehearsed. Every line below gets a date, a name and a verdict,
and an unrehearsed recovery is not a recovery.

### 1. Database restore from a dump

Row counts before and after, or it proves nothing.

```bash
# before: the dump, and the numbers it should reproduce
pg_dump --format=custom --dbname="$DATABASE_URL" --file=/data/backups/po_demo_$(date +%Y%m%d-%H%M).dump
psql "$DATABASE_URL" -At -c "select 'collectors',count(*) from collectors
  union all select 'tasks',count(*) from tasks
  union all select 'collection_sessions',count(*) from collection_sessions
  union all select 'episodes',count(*) from episodes
  union all select 'episode_reviews',count(*) from episode_reviews
  union all select 'settlements',count(*) from settlements
  union all select 'bills',count(*) from bills
  union all select 'payout_accounts',count(*) from payout_accounts order by 1" | tee /tmp/before.counts

# restore into a SEPARATE database, as its owner. Never restore over the demo
# database to rehearse restoring it.
createdb -U postgres po_demo_restore_test
pg_restore -U postgres --dbname=po_demo_restore_test /data/backups/<the dump>

# after: the same numbers, out of the restored copy
psql "postgres://postgres:...@<host>:5432/po_demo_restore_test" -At -c "<the same query>" | tee /tmp/after.counts
diff /tmp/before.counts /tmp/after.counts && echo "restore verified"
```

`diff` must print nothing. On Windows, `psql -At` output carries `\r`: strip it
(`tr -d '\r'`) before diffing, or the two files never match.

Drop `po_demo_restore_test` afterwards — `CHECKPOINT;` first, then
`DROP DATABASE` (plain, never `WITH (FORCE)`; the refusal while someone is
connected is the guard that stops you deleting a database another agent is
using).

**Rehearsed on ______ by ______: PASS / FAIL**

### 2. Certificate re-issue

```bash
cd /srv/playerone/deploy/cloud
docker compose logs caddy | tail -20            # what it says now
docker compose down
docker volume rm playerone_caddy_data           # the certificates live here
docker compose up -d
docker compose logs -f caddy                    # watch the ACME issuance
curl -sSI https://console.<domain>.vn/ | head -1
node deploy/demo-preflight.mjs                  # certificate-expiry reads PASS again
```

Restarting Caddy means restarting the API — they share a network namespace, so
`docker compose restart caddy api`, in that order, or every request answers 502.

**Do this once, deliberately, and not twice.** A fresh issuance on every start
is how a deployment meets Let's Encrypt's rate limit and then has no TLS at
all. Back up `caddy_data` with the database.

**Rehearsed on ______ by ______: PASS / FAIL**

### 3. The bucket's lifecycle rule is actually on the bucket

Set by hand, before the first upload, and therefore worth reading back rather
than assuming. Read-only:

```bash
aws s3api get-bucket-lifecycle-configuration \
  --endpoint-url "$STORAGE_ENDPOINT" --bucket "$STORAGE_BUCKET"
```

Expect both rules, `Enabled`: `abort-stale-multipart`
(`AbortIncompleteMultipartUpload`, 7 days, empty prefix = the whole bucket) and
`archive-billed-deliveries` (`Tag tier=archive` → the archive storage class).
See [RUNNING.md](../docs/RUNNING.md) for what each is for and which storage-class
strings HCM04 accepts. `NoSuchLifecycleConfiguration` means no rule is set:
`PutBucketLifecycleConfiguration` replaces the **whole** rule set, so put both
rules in one call and read it back again afterwards.

While you are there, the only way to see orphaned multipart cost:

```bash
aws s3api list-multipart-uploads --endpoint-url "$STORAGE_ENDPOINT" --bucket "$STORAGE_BUCKET"
```

**Rehearsed on ______ by ______: PASS / FAIL**

### 4. The cloud-loss walk

Not the commands read aloud — the walk. Start with the cloud origin up, pretend
it is gone, and get to a working demo on the centre PC: the operator runs the
two `check.mjs` commands and the two `run-*.cmd` scripts, all three browser
profiles move to the LAN origin, the LAN-origin APK is on the handset, and the
preflight passes against the centre database. Time it; if it takes longer than
ten minutes it is not a fallback you can reach mid-demo, and Thursday starts on
the centre PC instead.

**Rehearsed on ______ by ______: PASS / FAIL — minutes taken: ______**

### 5. The handset-upload-failure walk

Put the phone in airplane mode mid-upload, then deliver the same session
through **Engineering → Debug delivery** and watch the same episode reach
`ingested` in the console. This is also what proves the bucket's CORS rule is
applied for this console origin — the page cannot work without it, and the
failure is silent in every log.

**Rehearsed on ______ by ______: PASS / FAIL**

---

## Tuesday-evening failure: what we promise instead

The cutoff is real. If the **first complete cloud walkthrough fails on Tuesday
evening**, then:

1. Thursday is the **centre-PC variant**, decided Tuesday night and not
   revisited on Wednesday afternoon.
2. It is rehearsed **once, uninterrupted, on Wednesday**, against all five
   acceptance gates in
   [Fallback A](#fallback-a--cloud-loss-the-centre-pc-variant), before it is
   promised to anybody.
3. Stakeholders are told, before the meeting and again in the room, that **the
   cloud leg is deferred** — the platform runs at the upload centre, the
   Vietnam VM is not part of Thursday, and the date it becomes part of it is a
   commitment we make separately.
4. Wednesday 12:00 is scope freeze either way. After it: fixes only, no new
   screens, no new routes.

Nothing in this list is a downgrade of what is shown. The whole loop still
runs; it runs on one machine instead of two.

---

## What must be preserved

- **The demo database** (`po_demo_thursday`, or whatever `po_demo*` name this
  deployment uses). It holds the seeded identities, the reviewed episodes, the
  bill and the declared destination. Dump it after the final rehearsal and
  again after the demo; keep the dump off the VM.
- **`caddy_data`** — the certificates. Losing the volume means a fresh ACME
  issuance on every start, which is how a deployment meets Let's Encrypt's rate
  limit and then has no TLS at all. Back it up with the database.
- **The four demo secrets** (`PLAYERONE_DEMO_MACHINE_SECRET`,
  `_ADMIN_SECRET`, `_FINANCE_SECRET`, `_REVIEWER_SECRET`). The seed compares
  them on a rerun and refuses a different value rather than overwriting a
  sign-in; without them the rerun path is `bootstrap.ts --rotate-operator`.
- **`PLAYERONE_TOKEN_SECRET`.** Rotating it invalidates every session and every
  signed-in browser profile, mid-demo included.
- **The TF card, and the evidence bundle** from step 0:33. The card is never
  written to and never cleared; the bundle is what a stakeholder takes away.
- **The media root** — the imported `ego_*` sessions the review lane reads.

## What must never run against the demo database

Each of these truncates or rewrites, and each is correct against a throwaway
database:

| Never | What it does |
|---|---|
| `packages/api/scripts/seed-console.mjs` | truncates **every** table, then seeds a different console story |
| `packages/api/scripts/e2e-loop.mjs` | truncates every table; it is the loop assertion script |
| `packages/api/scripts/verify-e2e.mjs`, `verify-review.mjs` | assertion scripts; their data exists only for the run |
| `pnpm test` / `vitest` with `DATABASE_URL` pointing here | every database test file truncates, by design |
| `DROP DATABASE` / `WITH (FORCE)` | obvious, and it has happened |
| `PLAYERONE_ALLOW_SUPERUSER=1` with anything but a migration | the application connects as `playerone_app`; that is what keeps the append-only audit triggers honest |

`seed-stakeholder.mjs` and `demo-preflight.mjs` both refuse a database whose
name does not start `po_demo` or `playerone_demo`. That guard protects the
*other* databases from this demo; it does not protect this database from the
scripts above, which have no such guard. The name is the only thing between
them and Thursday — so keep the demo name, and keep every throwaway name
distinct (`po_e2e_*`, `po_test_*`).

Also never, in the room or out of it:

- **Do not flip `PLAYERONE_REVIEWER_MEDIA` to `1`** to make a point. It is a
  separate authorisation, it requires `PLAYERONE_SECURE_COOKIES=1`, and D11 has
  to be answered by Legal first.
- **Do not set `PLAYERONE_PAYOUT_MODE=api`.** With a sandbox environment the
  service refuses to boot at all, and that refusal is deliberate.
- **Do not simulate a payment.** See
  [0:28](#028--the-payment-and-the-honest-ending--finance).
